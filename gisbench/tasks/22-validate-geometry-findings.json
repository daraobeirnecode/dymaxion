{
  "schema_version": "1.0.0",
  "id": "validate-geometry-findings",
  "capability": "validate_spatial_data",
  "input": { "fixture": "spatial-validation/geometry-findings.geojson" },
  "expected": { "outcome": "success", "golden_file": "validate-geometry-findings.json" },
  "tolerances": { "numeric_absolute": 0, "normalized_fields": ["$.output.report.source_uri", "$.output.report.source_handle", "$.output.evidence.source.uri", "$.output.evidence.source.identity.value", "$.output.evidence.parameters.canonical_json", "$.output.evidence.parameters.sha256", "$.output.evidence.outputs[0].sha256"] },
  "allowed_operations": ["boundary_preflight", "stat", "read_file", "parse_geojson", "hash_sha256", "derive_validation_report"],
  "expected_approval": { "required": false, "decision": "not-requested" }
}
